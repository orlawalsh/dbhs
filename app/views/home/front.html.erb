<div class="row">
  <div id="newsfeed" class="col-lg-6">
    <h4><%= fa_icon "newspaper-o" %> NEWSFEED</h4>
    <div id="activities">
      <% if @activities.empty? %>
        <div class="well">
          <%= render('shared/no_resource', resources: 'Activities') %>
        </div>
      <% else %>
        <%= render_activities(@activities, layout: '/shared/activity') %>
      <% end %>
    </div>
    <%= render 'shared/paginate', resources: @activities %>
  </div>
  <div id="intro" class="col-lg-6">
    <h1>Diabetes Helper System</h1>
   <p>Diabetes Helper System is a forum style social media platform. The aim is to conect diabetics together on one platform, where they can connect with other people similar to them, share recipes, ideas and just generally support each other and meet like-minded people.</p>
   <hr>
   <p>Why not sign up today?</p>
<%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary" %>
         

  </div>
</div>
